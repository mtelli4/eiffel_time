import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  button: {
    borderRadius: 10,
  },
  text: {
    textAlign: 'center',
    fontFamily: 'Montserrat',
    fontWeight: 500,
  },
})