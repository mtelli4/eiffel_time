import Feather from 'react-native-vector-icons/Feather'
import { TouchableOpacity } from "react-native"

const iconButton = {
  marginLeft: 8,
}

export default function PlusButton() {
  return (
    <Feather name="plus" size={16} color="#2E3494" />
  )
}
